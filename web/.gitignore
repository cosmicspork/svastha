# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated by wasm-pack from crates/wasm (see package.json "wasm" script).
src/wasm

# Generated from the root CHANGELOG.md on every build (see package.json
# "build" script and scripts/build-changelog) — regenerates deterministically,
# so it's never committed.
public/changelog.json

# Playwright e2e outputs (local only)
test-results
playwright-report

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
